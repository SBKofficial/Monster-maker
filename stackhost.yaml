# User Info
User:
  name: "Aritra"
  github: "safari"

# Runtime Config
runtime:
  image: "python:3.11-slim"

# Build & Start
commands:
  build:
    - "pip install -r requirements.txt"
  start: "python main.py" 

# Note: 'env' section removed. 
# The server will automatically load the secrets you added in the Dashboard.

repository:
  branch: "main"
  auto_deploy: true
  
